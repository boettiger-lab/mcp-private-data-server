# Available Datasets (Private)

**IMPORTANT** You must read remote parquet datasets with read_parquet()

---

**1. Wyoming (Private)**
- Partitioned parquet files at `s3://private-wyoming/**`
- *(Dataset details — columns, description, join keys — to be filled in once bucket is created)*

